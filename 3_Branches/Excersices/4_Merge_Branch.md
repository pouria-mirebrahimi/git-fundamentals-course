## Exercise 4: Merge Branches

**Instructions:**

1. Switch to `main`:

```bash
git switch main
```

2. Merge `feature1`:

```bash
git merge feature1
```

**Expected Outcome:**

* `feature1.txt` appears in `main`
* Merge is fast-forward if main hasn’t changed