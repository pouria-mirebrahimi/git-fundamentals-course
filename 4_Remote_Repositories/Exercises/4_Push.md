## Exercise 4: Push Local Commits

**Objective:** Push your local commits to remote.

**Instructions:**

1. Create a file:

```bash
echo "Remote test" > test.txt
git add test.txt
git commit -m "Add test file"
```

2. Push to remote:

```bash
git push origin main
```

**Expected Outcome:**

* Commit appears in remote repository