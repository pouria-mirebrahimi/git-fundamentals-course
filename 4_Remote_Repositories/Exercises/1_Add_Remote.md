## Exercise 1: Add a Remote

**Objective:** Add a remote repository to your local repo.

**Instructions:**
```bash
git remote add origin https://github.com/your-username/remote-repo.git
git remote -v
````

**Expected Outcome:**

* Remote `origin` is listed with the URL